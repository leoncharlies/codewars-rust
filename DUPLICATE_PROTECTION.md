# 防止重复添加题目 - 功能说明

## 问题背景

你可能会忘记之前是否做过某道题，如果不小心直接运行 `./add_kata.sh <kata_id>` 而忘记加 `--review`，会不会覆盖之前的代码？

## 解决方案

### ✅ 完全不会覆盖！系统有三重保护：

#### 1️⃣ 文件系统检测
脚本会扫描所有 `katas/` 目录，查找是否已存在该 kata ID 的文件夹。

#### 2️⃣ 索引文件检测  
维护一个 `.kata_index` 文件，记录所有做过的题目：
```
kata_id|题目名称|难度|路径|完成日期
5541f58a944b85ce6d00006a|Product of consecutive Fib numbers|5kyu|katas/5kyu/005_...|2025-12-02
```

#### 3️⃣ 友好的警告提示
如果题目已存在，会显示详细信息：

```bash
$ ./add_kata.sh 5541f58a944b85ce6d00006a

⚠️  警告: 你已经做过这道题目了！

📋 索引记录:
   5541f58a944b85ce6d00006a
   product of consecutive fib numbers
   5kyu
   katas/5kyu/005_product_of_consecutive_fib_numbers_5541f58a944b85ce6d00006a
   2025-12-02

📁 文件位置: katas/5kyu/005_product_of_consecutive_fib_numbers_5541f58a944b85ce6d00006a
🔄 已复习: 2 次

如果要添加复习版本，请使用:
  ./add_kata.sh 5541f58a944b85ce6d00006a --review
```

**脚本会直接退出，不会做任何修改！**

---

## 新增功能

### 1. 快速检查是否做过某题

```bash
./manage_kata.sh check <kata_id>
```

**示例 - 已做过的题：**
```bash
$ ./manage_kata.sh check 5541f58a944b85ce6d00006a

✅ 已完成这道题

详细信息:
  题目ID:    5541f58a944b85ce6d00006a
  题目名称:  product of consecutive fib numbers
  难度:      5kyu
  路径:      katas/5kyu/005_product_of_consecutive_fib_numbers_5541f58a944b85ce6d00006a
  完成日期:  2025-12-02
  复习次数:  2
```

**示例 - 未做过的题：**
```bash
$ ./manage_kata.sh check 54da5a58ea159efa38000836

❌ 还没做过这道题

添加题目:
  ./add_kata.sh 54da5a58ea159efa38000836
```

### 2. 查看所有题目索引

```bash
./manage_kata.sh index
```

输出示例：
```
=== Codewars 题目索引 ===

格式: kata_id | 题目名称 | 难度 | 路径 | 完成日期

5541f58a944b85ce6d00006a | Product of consecutive Fib numbers | 5kyu | 2025-12-02
54b42f9314d9229fd6000d9c | Duplicate Encoder                  | 6kyu | 2025-12-02
5259b20d6021e9e14c0010d4 | Reverse words                      | 7kyu | 2025-12-02
...
```

---

## 索引文件维护

### 位置
`.kata_index` - 在工作空间根目录

### 格式
```
kata_id|题目名称|难度|路径|完成日期
```

### 自动维护
- ✅ 添加新题目时自动写入
- ✅ 启动时自动检查
- ✅ 已加入 `.gitignore`（可以选择提交或忽略）

### 手动重建索引
如果索引文件丢失或不准确，可以重建：

```bash
rm .kata_index  # 删除旧索引

# 遍历所有题目重建索引
for kata_dir in katas/*/*; do
  if [ -d "$kata_dir" ] && [ -f "$kata_dir/src/lib.rs" ]; then
    KATA_ID=$(grep "^//! Kata ID:" "$kata_dir/src/lib.rs" | sed 's/^\/\/! Kata ID: //')
    TITLE=$(grep "^//! Title:" "$kata_dir/src/lib.rs" | sed 's/^\/\/! Title: //')
    RANK=$(grep "^//! Rank:" "$kata_dir/src/lib.rs" | sed 's/^\/\/! Rank: //')
    COMPLETED=$(grep "^//! Completed:" "$kata_dir/src/lib.rs" | sed 's/^\/\/! Completed: //')
    echo "$KATA_ID|$TITLE|$RANK|$kata_dir|$COMPLETED" >> .kata_index
  fi
done
```

---

## 使用场景

### 场景 1：不确定是否做过
```bash
# 先检查
./manage_kata.sh check 5541f58a944b85ce6d00006a

# 如果提示"❌ 还没做过"，再添加
./add_kata.sh 5541f58a944b85ce6d00006a
```

### 场景 2：批量检查题目
从浏览器复制一批题目 ID，逐个检查：
```bash
for id in 5541f58a944b85ce6d00006a 54b42f9314d9229fd6000d9c 5259b20d6021e9e14c0010d4; do
  echo "=== Checking $id ==="
  ./manage_kata.sh check "$id"
  echo ""
done
```

### 场景 3：查看索引找题
```bash
# 查看所有题目
./manage_kata.sh index

# 搜索特定题目
./manage_kata.sh index | grep "fibonacci"

# 查看某个难度的所有题目
./manage_kata.sh index | grep "5kyu"
```

---

## 常见问题

### Q: 如果我误删了题目文件夹，索引会自动更新吗？
A: 不会自动更新。但脚本使用**双重检测**（文件系统 + 索引），即使索引有误，文件系统检测仍会保护你。

建议删除题目后手动更新索引：
```bash
# 从索引中删除该行
sed -i '/5541f58a944b85ce6d00006a/d' .kata_index
```

或者重建整个索引（见上面的"手动重建索引"）。

### Q: 索引文件应该提交到 Git 吗？
A: 看个人喜好：
- **提交**：可以在不同机器间同步题目记录
- **不提交**：每台机器独立维护，更灵活

目前索引已加入 `.gitignore`，如果要提交：
```bash
git add -f .kata_index
git commit -m "Add kata index"
```

### Q: 如果手动移动了题目目录怎么办？
A: 重建索引即可。索引是从文件系统扫描生成的。

---

## 总结

✅ **完全不会覆盖旧题目**  
✅ **三重保护机制**  
✅ **友好的错误提示**  
✅ **方便的检查工具**  
✅ **完整的题目索引**  

放心使用，不用担心数据丢失！🎉
